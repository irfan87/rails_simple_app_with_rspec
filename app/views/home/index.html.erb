<%= link_to 'New Post', new_post_path %>
<div>
  <ul>
    <% @posts.each do |post| %>
      <li>
        <h2><%= post.title %></h2>
        <p><%= post.body %></p>
        <div>
          <%= link_to 'Show this post', post_path(post) %>
        </div>
      </li>
    <% end %>
  </ul>
</div>
