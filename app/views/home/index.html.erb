<%= link_to 'New Post', new_post_path %>
<div>
  <ul>
    <% @posts.each do |post| %>
      <li>
        <p>
          <%= link_to post.title, post_path(post) %>
          - <%= link_to 'Edit', edit_post_path(post) %>
        </p>
      </li>
    <% end %>
  </ul>
</div>
